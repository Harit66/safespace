import { Box } from "@mui/material";
import React from "react";

type Props = {
  userType: string;
};

const SideBar = ({ userType }: Props) => {
  return <Box>SideBar</Box>;
};

export default SideBar;
